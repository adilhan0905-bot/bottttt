python-telegram-bot>=20.8,<21.0    # Async Telegram Bot API
python-dotenv>=1.0.0               # Загрузка .env (BOT_TOKEN, MANAGER_CHAT_ID)

SQLAlchemy>=2.0.0                  # ORM и миграции
aiosqlite>=0.19.0                  # Async-драйвер SQLite для SQLAlchemy
alembic>=1.13.0                    # Миграции БД (опционально, но рекомендуется)

gspread>=6.0.0                     # Работа с Google Таблицами
# По ТЗ требуется oauth2client, но он устарел.
# Рекомендуется использовать google-auth вместе с gspread:
google-auth>=2.22.0                # Современная OAuth2 аутентификация
google-auth-oauthlib>=1.0.0        # Поток авторизации (если нужен браузерный OAuth)
# Если строго по ТЗ — оставь строку ниже, но лучше мигрировать на google-auth:
# oauth2client>=4.1.3                # [LEGACY] только если друг настаивает

openpyxl>=3.1.0                    # Резервный экспорт, если таблица не доступна

typing-extensions>=4.7.0           # Расширенные типы (Optional, Required и т.д.)